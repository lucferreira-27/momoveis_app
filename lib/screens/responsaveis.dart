import 'package:flutter/material.dart';

class Responsaveis  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Responsaveis")),
    );
  }
}